include <hinge_version.scad>;

// Closed-position inspection:
// - fixed template at z=0..2 mm;
// - moving enclosure front rim at z=2.6 mm;
// - continuous lower apron/base continues to y=0.5 mm;
// - concealed 6 mm rail hinge is fully internal at y=11.5, z=10.5 mm;
// - only the internal front/lower apron quadrant is relieved for hinge sweep.
hinge_version_assembly(0);
